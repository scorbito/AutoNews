# auto_news 의존성
# 설치: pip install -r requirements.txt

# 웹/서버
fastapi>=0.136.1
uvicorn[standard]>=0.42.0
jinja2>=3.1.6
python-multipart>=0.0.22
python-dotenv>=1.2.2
itsdangerous>=2.2          # 세션 쿠키 서명(SessionMiddleware)

# 메일 수집
imap-tools>=1.13.0

# 추출기
olefile>=0.47           # HWP(OLE2)
PyMuPDF>=1.27.2         # PDF
python-docx>=1.2.0      # DOCX
trafilatura>=2.1.0      # 웹링크
pillow>=12.2.0          # 이미지 크기/검증

# LLM (Gemini)
google-genai>=1.69.0

# 발행/다운로드
requests>=2.33.1

# DB (Supabase PostgreSQL)
psycopg[binary]>=3.2

# 테넌트 비밀번호 암호화
cryptography>=43
